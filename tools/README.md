# Protea Developer Tools

Developer tools live here.

Planned tools:

- host capability detection
- hardware-tier detection
- state migration
- image verification
- release validation

Tools must be safe to run as an unprivileged user unless root is explicitly required by the target operation.
